import { generatorProperties } from './properties.shared';

/** 拔高 / 创新 math templates: every answer re-derived by the template's own independent solver. */
generatorProperties((id) => id.endsWith('.challenge'));
