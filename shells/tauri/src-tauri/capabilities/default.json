{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: no native APIs are exposed to the web app",
  "windows": ["main"],
  "permissions": ["core:default"]
}
